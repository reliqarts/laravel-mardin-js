/**
 * Mardin
 *
 * Styled Thread Component
 *
 * @type styled('div')
 */

import styled from 'react-emotion';
import common from '../css/common.scss';

export default styled('div')`
    ${common} .sec-main {
        padding: 0;
        width: 100%;
    }
`;
